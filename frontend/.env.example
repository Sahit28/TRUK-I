# Cambia esta URL a la del backend en producción cuando despliegues en Render
VITE_API_URL=http://localhost:5000/api

const apiUrl = import.meta.env.VITE_API_URL;
axios.post(`${apiUrl}/registro`, data);
